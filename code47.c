#include <stdio.h>
//Dynamic Memory Allocation
//It is a way to allocate the memoris to a data structure during runtime ,we can use DMA
//function available in c to allocate and free memory dusring runtime.
/*
            <-----Functions For DMA in C----->
1. malloc()->malloc stands for memory allocation.It takes no.of bytes to be allocated as an input and return a pointer of type void.
2. calloc()->caloc stand for caontinuous memory allocation.It initialize each memory block with a default valur of zero,it take two arg
3. free()->for de-allacation of memory ,for less memory consumption,we can use later same memory
4. realloc
*/

int main()
{

    return 0;
}
